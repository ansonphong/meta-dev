---
name: dev
description: Alias of /meta-dev — identical command (pure redirect: `Execute /meta-dev $ARGUMENTS`). /dev and /meta-dev are the SAME skill — invoke either, there is nothing to choose between them.
argument-hint: <subject> [--phase brainstorm|research|plan|gaps|execute|review]
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Agent]
model: opus
---

# /dev → /meta-dev

Execute `/meta-dev $ARGUMENTS`.
