---
name: eval
description: Shortcut for /meta-eval — Dedicated evaluator agent, tests implementations against design criteria
argument-hint: <plan-path | feature-name>
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Agent]
model: sonnet
---

# /eval → /meta-eval

Execute `/meta-eval $ARGUMENTS`.
