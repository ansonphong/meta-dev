---
name: eval
description: "Alias of /meta-eval — identical command (pure redirect: `Execute /meta-eval $ARGUMENTS`). /eval and /meta-eval are the SAME skill — invoke either, there is nothing to choose between them."
argument-hint: <plan-path | feature-name>
allowed-tools: [Skill, Read, Write, Edit, Bash, Glob, Grep, Agent]
model: opus
---
Execute /meta-eval $ARGUMENTS
