---
name: ux
description: Shortcut for /meta-ux — UX evaluation against the configured design system (heuristics + design-system + accessibility), with iterative fix rounds
argument-hint: [plan-path | "running app" | feature:<name> | <repo>] [--depth shallow|standard|deep] [--focus <area>] [--rounds N]
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Agent]
model: sonnet
---

# /ux → /meta-ux

Execute `/meta-ux $ARGUMENTS`.
