---
name: version
description: Alias of /meta-version — identical command (pure redirect: `Execute /meta-version $ARGUMENTS`). /version and /meta-version are the SAME skill — invoke either, there is nothing to choose between them.
argument-hint: [status | bump <level> | sync | config]
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep]
model: opus
---

# /version → /meta-version

Execute `/meta-version $ARGUMENTS`.
