---
name: audit
description: Shortcut for /meta-audit — Harness simplification audit, tests whether pipeline components are still load-bearing
argument-hint: [full | component:<name>] [--compare] [--force-full]
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Agent]
model: sonnet
---

# /audit → /meta-audit

Execute `/meta-audit $ARGUMENTS`.
